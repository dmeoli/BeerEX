# Copy to .env and fill in. Never commit your real token.
TOKEN=your-telegram-bot-token-from-@BotFather
# Reasoning backend: fuzzy (data-grounded, default) | cf (certainty factors)
BEEREX_BACKEND=fuzzy
